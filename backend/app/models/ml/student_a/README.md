# Student A — Trip Duration Model Directory

This directory is designated for Student A's XGBoost V3 Trip Duration prediction model artifacts.

## Model Contract & Files
- Model file: `xgboost_trip_duration_v3.json` (or `.json` / `.model` format)
- Feature columns file: `v3_feature_columns.json` (44 aligned XGBoost features)

## Reference Path
The model is loaded by `backend/app/services/trip_duration_service.py`.
Active model artifact path: `backend/models/trip_duration/xgboost_trip_duration_v3.json`.
